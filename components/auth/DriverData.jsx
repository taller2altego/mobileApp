import React, { useState } from "react";
import { Button, Pressable, Text, TextInput, View } from "react-native";
import { post } from "../../utils/requests";
import styles from "../styles";

export default function DriverData({navigation}) {
  return (
    <View>
    </View>
  );
}
